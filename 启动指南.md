# 跨境铺货 SaaS 平台 — 启动指南

## 前置条件

你需要准备：
1. 一台电脑（Windows 即可）
2. 安装 Docker Desktop（免费）
3. 各平台 API Key（见下方）

---

## 第一步：安装 Docker Desktop

1. 下载 Docker Desktop：https://www.docker.com/products/docker-desktop/
2. 安装后重启电脑
3. 打开 Docker Desktop 确认它在运行（系统托盘有鲸鱼图标）

验证：打开命令行输入：
```
docker --version
docker compose version
```
能看到版本号就行。

---

## 第二步：配置 API Key

在 `E:\cross_border_tool\backend\` 目录下，复制一份配置文件：

```
复制 .env.example 为 .env
```

然后用记事本打开 `.env`，填写以下信息：

```ini
# 数据库（不用改，Docker会自动创建）
DATABASE_URL=postgresql://user:password@db:5432/cross_border

# Redis（不用改）
REDIS_URL=redis://redis:6379/0

# OpenAI（必填 —— 用于AI生成标题/描述）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxx
# 获取方式：https://platform.openai.com/api-keys 注册后创建

# fal.ai（必填 —— 用于AI场景图生成）
FAL_KEY=xxxxxxxxxxxxxxx
# 获取方式：https://fal.ai/dashboard/keys 注册后创建

# 1688开放平台（必填 —— 用于获取商品数据）
ALIBABA_APP_KEY=xxxxxxx
ALIBABA_APP_SECRET=xxxxxxx
# 获取方式：https://open.1688.com 入驻开发者 → 创建应用

# 速卖通开放平台（必填 —— 用于上传商品）
ALIEXPRESS_APP_KEY=xxxxxxx
ALIEXPRESS_APP_SECRET=xxxxxxx
ALIEXPRESS_ACCESS_TOKEN=xxxxxxx
# 获取方式：https://developers.aliexpress.com 创建应用 → OAuth授权

# 阿里云OSS（必填 —— 用于存储AI生成的图片）
OSS_ACCESS_KEY_ID=xxxxxxx
OSS_ACCESS_KEY_SECRET=xxxxxxx
OSS_BUCKET_NAME=你的bucket名称
OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
# 获取方式：阿里云控制台 → 对象存储OSS → 创建Bucket

# JWT密钥（随便写一串字符就行）
SECRET_KEY=my-super-secret-key-12345
```

---

## 第三步：启动项目

打开命令行（cmd 或 PowerShell），进入项目目录：

```
cd E:\cross_border_tool
docker compose up -d
```

第一次启动会下载镜像，大约需要 5-10 分钟。

启动完毕后验证：
```
docker compose ps
```
应该看到 5 个服务都是 running 状态：
- db（数据库）
- redis（缓存/队列）
- backend（后端API）
- worker（AI处理队列）
- frontend（前端网页）

---

## 第四步：打开使用

浏览器打开：

```
http://localhost:3000
```

这就是你的铺货平台首页。

---

## 使用流程

### 1. 新建批次
- 点击首页右上角「新建批次」
- 输入批次名称（如：夏季女装第1批）
- 设置定价倍率（如 3.0 = 1688价格×3 作为速卖通售价）
- 粘贴 1688 商品链接（每行一个，支持100-500个）
- 点击「开始批量处理」

### 2. 等待AI处理
- 系统自动从 1688 获取数据
- AI 自动生成英文标题（核心卖点+对象+特征+热搜词+长尾词）
- AI 自动生成产品描述（特征/洗护/尺码/场景 + 中文翻译）
- AI 自动生成9张主图 + 10张详情图 + 白底图 + 场景图
- 状态从 pending → fetching → ai_processing → ready（绿色）
- 100个商品大约 30-60 分钟完成

### 3. 预览/编辑
- 状态变为「就绪」后，点击「编辑」进入速卖通风格编辑器
- 7个Tab：基本信息 / 商品属性 / 商品图片 / SKU规格 / 商品详情 / 物流 / 售后
- AI已自动填好所有字段，可以直接用或者微调

### 4. 一键上传
- 回到批次详情页
- 勾选要上传的商品（或点「一键上传全部就绪」）
- 系统自动通过速卖通API批量上传
- 上传成功后状态变为「已上传」（蓝色）

---

## 常用操作

| 操作 | 地址 |
|------|------|
| 前端界面 | http://localhost:3000 |
| 后端API文档 | http://localhost:8000/docs |
| 查看日志 | `docker compose logs -f worker`（看AI处理进度） |
| 重启服务 | `docker compose restart` |
| 停止服务 | `docker compose down` |
| 完全重来 | `docker compose down -v`（会删除数据库） |

---

## API Key 申请指南（简要）

### OpenAI（AI标题/描述）
1. 打开 https://platform.openai.com
2. 注册/登录
3. 左侧 API Keys → Create new key
4. 充值 $10-20 即可用很久（GPT-4o-mini 很便宜）

### fal.ai（AI图片生成）
1. 打开 https://fal.ai
2. 注册/登录
3. Dashboard → Keys → Create
4. 充值 $10-20（Flux Pro约$0.04/张图）

### 1688开放平台
1. 打开 https://open.1688.com
2. 用1688账号登录
3. 入驻成为开发者
4. 创建应用 → 获得 App Key / Secret
5. 申请商品查询API权限

### 速卖通开放平台
1. 打开 https://developers.aliexpress.com
2. 用速卖通卖家账号登录
3. 创建应用 → 获得 App Key / Secret
4. OAuth 授权流程获取 Access Token
5. 你已经有开发者账号，应该熟悉这个流程

### 阿里云OSS
1. 打开 https://www.aliyun.com → 产品 → 对象存储OSS
2. 创建 Bucket（选公共读，地域选杭州）
3. AccessKey管理 → 创建 AccessKey
4. 记下 Key ID 和 Secret

---

## 成本估算

| 项目 | 100个商品 | 500个商品 |
|------|-----------|-----------|
| OpenAI 标题+描述 | ≈¥3 | ≈¥15 |
| fal.ai 图片 | ≈¥450 | ≈¥2,250 |
| 阿里云OSS | ≈¥5 | ≈¥25 |
| **合计** | **≈¥458** | **≈¥2,290** |

约 ¥4.6/商品，对比人工每个商品 30-60 分钟工时，极其划算。
